/** default board size, tic tac toe is a 3x3 board */
export const BOARD_SIZE = 3;

/** start player is X */
export const START_PLAYER = 0;

/** values for turns */
export enum TurnList {
    x = 0,
    o = 1,
};
